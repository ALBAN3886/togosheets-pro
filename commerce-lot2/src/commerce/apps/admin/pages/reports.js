import { emptyState } from '../../../shared-ui/components/empty-state.js';

export function renderReportsPage() {
  return emptyState('Rapports', 'Branchez ici vos rapports journaliers, mensuels et exports.');
}
