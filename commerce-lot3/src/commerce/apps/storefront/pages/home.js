export function renderStorefrontHome(storeName = 'Boutique Publique') {
  return `<section class="cm-card cm-section"><h1>${storeName}</h1><p style="color:var(--cm-muted)">Catalogue Commerce synchronisé en temps réel avec Firebase.</p></section>`;
}
