export function renderStorefrontHome(storeName = 'Boutique') {
  return `<section class="cm-card cm-section"><h1>${storeName}</h1><p style="color:var(--cm-muted)">Catalogue synchronisé en temps réel depuis Firebase.</p></section>`;
}
